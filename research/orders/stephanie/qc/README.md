# Stephanie — order assets

Assets for Stephanie Tran's order (handle `tranimal`). Timeline + IDs live in `../../stephanie.md`.

- `qc/<#>-<slug>/` — quality-check photos from the Superbuy warehouse, one folder per garment, prefixed with the friend-facing product number from the ordering table (e.g. `qc/1-the-row-frank-jacket/`). These are what we forward to Stephanie in "quick update" emails / the future "QC in cart" feature.
- Drop email screenshots / receipts / parcel photos at this level (`research/orders/stephanie/`).

Product numbers are provisional until her final paid list locks, then freeze them.
