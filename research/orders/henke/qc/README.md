# Henke — order assets

Assets for Henrik Zachrisson's order (handle `u38403`). Timeline + IDs live in `../../henke.md`.

- `qc/<item-slug>/` — quality-check photos from the Superbuy warehouse, one folder per garment (e.g. `qc/mm-collarless-jacket/`). These are what we forward to Henke in "quick update" emails.
- Drop email screenshots / receipts / parcel photos at this level (`research/orders/henke/`).

Naming: use the same item names as the ordering table in `henke.md` so photos map 1:1 to line items.
